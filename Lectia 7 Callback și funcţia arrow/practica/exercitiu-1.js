"use strict";
// Exemplul 1 - Funcția callback

// Scrie următoarele funcții:

// - `createProduct(obj, callback)` - acceptă un obiect produs fără un id, și de asemenea un
// callback. Funcția creează un obiect produs adăugându-i un identificator unic în
// proprietatea `id` și apelează callback-ul, transmițându-i obiectul creat.

// - `logProduct(product)` - callback accepting a product object and logging it to
//   console

// - `logTotalPrice(product)` - callback receiving product object and logging
//   the total value of the item in the console
